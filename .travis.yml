language: cpp

compiler:
  - gcc

before_install:
  - echo $LANG
  - echo $LC_ALL
  - sudo apt-get update && sudo apt-get install libsdl2-dev libsdl2-image-dev



branches:
  only:
    - master

os:
  - linux
