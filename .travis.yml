language: cpp

compiler:
  - gcc

before_install:
  - echo $LANG
  - echo $LC_ALL
  - if [ $TRAVIS_OS_NAME == linux ]; then sudo apt-get update && sudo apt-get install libsdl2-dev  libsdl2-image-dev; fi



branches:
  only:
    - master

os:
  - linux
